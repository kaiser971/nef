{% macro img(attribut, url, alt) %}

    <img {{ attribut }} src={{ url }} alt={{ alt }}/>

{% endmacro %}

{% macro video(attribut, src, sourceAttr) %}

    <video {{ attribut }}>
        <source src={{ src }} {{ sourceAttr }}>
    </video>

{% endmacro %}